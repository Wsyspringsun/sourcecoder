<!DOCTYPE html>
<html>
  <head>
	  <%include ../head.ejs%>
  </head>
 <body>
    <%include ../nav.ejs%>
    <div class="jumbotron">
      <div class="container">
        <p></p>
        <form class="form-horizontal" role="form">
          <input type="text" placeholder="关键字" class="form-control">
          <button type="submit" style="float:left;" class="btn">搜索</button>
        </form>
      </div>
    </div>

    <div id="primaryContent" class="container">
      <!-- Example row of columns -->
      <div class="row">
        <div class="blog-content">
	</div>
        <div class="col-md-4">
          <h2>最新</h2>
	  <ul>
	      <% nearly.forEach(function(blog){%>
	      <li><a href="#" class="blog"
			      blog-id="<%=blog.url%>"><%=blog.title%></a>&nbsp;&nbsp;
		      <a href="javascript:void(0)" blog-id="<%=blog.url%>">赞</a>&nbsp;&nbsp;<a href="#">评论</a></li>
	      <% })  %>
	  </ul>
        </div>
        <div class="col-md-4">
          <h2>人气</h2>
	  <ul>
	      <% nearly.forEach(function(blog){%>
	      <li><a href="#" class="blog"
			      blog-id="<%=blog.url%>"><%=blog.title%></a>&nbsp;&nbsp;
		      <a href="javascript:void(0)" blog-id="<%=blog.url%>">赞</a>&nbsp;&nbsp;<a href="#">评论</a></li>
	      <% })  %>
       </div>
        <div class="col-md-4">
          <h2>推荐</h2>
	  <ul>
	      <% nearly.forEach(function(blog){%>
	      <li><a href="#" class="blog"
			      blog-id="<%=blog.url%>"><%=blog.title%></a>&nbsp;&nbsp;
		      <a href="javascript:void(0)" blog-id="<%=blog.url%>">赞</a>&nbsp;&nbsp;<a href="#">评论</a></li>
	      <% })  %>
        </div>
      </div>
      <hr>

      <footer>
      <p>&copy; 源编码 <script>document.write(new Date().getFullYear());</script></p>
      </footer>
    </div>
    <!-- /container -->


      <%include ../scripts.ejs%>
      <script>
        $(function(){
            $('.blog').click(function(){
                var me = $(this);
                var title = me.text();
                $.get('blog/GoInstall',function(rlt){
		    var rlt = '<span class="glyphicon glyphicon-off"></span>' + rlt;
		
                    var div = $('.blog-content').html(rlt);
		    div.children('.glyphicon-off').click(function(){
			    div.html('');
		    });
                });
            });
        });
      </script>
  </body>
</html>
